services:
    db:
        image: postgres:18-alpine
        container_name: pokemon-dev-db
        restart: unless-stopped
        environment:
            POSTGRES_PASSWORD: postgres
            POSTGRES_USER: postgres
        ports:
            - '5433:5432'
