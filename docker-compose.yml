services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: apoco-backend-challenge
    environment:
      HOST: "0.0.0.0"
      PORT: "8080"
    ports:
      - "8080:8080"
    restart: unless-stopped
